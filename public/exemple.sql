-- SELECT * FROM `detail` INNER JOIN `plat` on `detail`.`plat_id`= `plat`.id WHERE `detail`.quantite > 1;

--SELECT `detail`.`plat_id` FROM `detail` INNER JOIN `plat` on `detail`.`plat_id`= `plat`.id WHERE `detail`.quantite > 1; 

--SELECT DISTINCT `detail`.`plat_id` FROM `detail` INNER JOIN `plat` on `detail`.`plat_id`= `plat`.id WHERE `detail`.quantite > 1; 